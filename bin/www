Skip to content
Search or jump to…

Pull requests
Issues
Marketplace
Explore
 
@FarahMo24 
FarahMo24
/
Scrabble
1
0
0
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
Scrabble/bin/www /
@CarbonMoKnoxide13
CarbonMoKnoxide13 removed console logs and comments and fixed minor create game bug
Latest commit 669bc7b on May 18, 2020
 History
 1 contributor
Executable File  59 lines (47 sloc)  1.11 KB
  
#!/usr/bin/env node

var app = require('../app');
var debug = require('debug')('test-application-2:server');
var http = require('http');

var port = normalizePort(process.env.PORT || '3000');
app.set('port', port);

var server = http.createServer(app);

server.listen(port);
server.on('error', onError);
server.on('listening', onListening);

function normalizePort(val) {
  var port = parseInt(val, 10);

  if (isNaN(port)) {
    return val;
  }

  if (port >= 0) {
    return port;
  }

  return false;
}

function onError(error) {
  if (error.syscall !== 'listen') {
    throw error;
  }

  var bind = typeof port === 'string'
    ? 'Pipe ' + port
    : 'Port ' + port;

  switch (error.code) {
    case 'EACCES':
      console.error(bind + ' requires elevated privileges');
      process.exit(1);
      break;
    case 'EADDRINUSE':
      console.error(bind + ' is already in use');
      process.exit(1);
      break;
    default:
      throw error;
  }
}

function onListening() {
  var addr = server.address();
  var bind = typeof addr === 'string'
    ? 'pipe ' + addr
    : 'port ' + addr.port;
  debug('Listening on ' + bind);
}
© 2021 GitHub, Inc.
Terms
Privacy
Security
Status
Docs
Contact GitHub
Pricing
API
Training
Blog
About
Loading complete
